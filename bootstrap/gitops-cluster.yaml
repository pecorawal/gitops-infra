apiVersion: apps.open-cluster-management.io/v1beta1
kind: GitOpsCluster
metadata:
  name: argo-acm-integration
  namespace: openshift-gitops
spec:
  deployerId: openshift-gitops
  placementRef:
    kind: Placement
    apiVersion: cluster.open-cluster-management.io/v1beta1
    name: all-managed-clusters # Este Placement deve selecionar os clusters do Set

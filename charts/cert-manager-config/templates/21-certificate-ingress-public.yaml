{{- if and .Values.certManager.enabled .Values.ingress.public.enabled .Values.ingress.public.certificate.enabled }}
# Certificado wildcard do IngressController PUBLICO.
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.ingress.public.certificate.secretName }}
  namespace: openshift-ingress
  annotations:
    argocd.argoproj.io/sync-wave: "3"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  secretName: {{ .Values.ingress.public.certificate.secretName }}
  commonName: {{ printf "*.%s" .Values.ingress.public.domain | quote }}
  dnsNames:
    - {{ printf "*.%s" .Values.ingress.public.domain | quote }}
    - {{ .Values.ingress.public.domain | quote }}
  privateKey:
    algorithm: RSA
    size: 2048
    rotationPolicy: Always
  usages:
    - server auth
    - digital signature
    - key encipherment
  issuerRef:
    name: {{ .Values.ingress.public.certificate.issuer }}
    kind: ClusterIssuer
    group: cert-manager.io
{{- end }}

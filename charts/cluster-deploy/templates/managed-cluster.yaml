apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  name: {{ .Values.clusterName }}
  labels:
    {{- toYaml .Values.labels | nindent 4 }}
    # Label que identifica o vendor para o ACM
    vendor: {{ .Values.cloudType }}
spec:
  hubAcceptsClient: true



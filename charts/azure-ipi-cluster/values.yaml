# Defaults. Os valores reais vem de clusters/<cluster>/values.yaml.
clusterName: ""
environment: dev
baseDomain: ""
# Vazio = resolvido a partir de labels.env por clusterSets.byEnv (_clusterset.tpl).
# Preencher aqui e o escape hatch para forcar um set especifico.
clusterSet: ""
clusterSets:
  default: ""
  byEnv: {}
cloudProvider: Azure
labels: {}
addons:
  policyController: true
  searchCollector: true
  applicationManager: true
  certPolicyController: true

provision:
  enabled: false
  imageSetRef: ""
  credentials:
    mode: existing                # existing | externalSecret (este exige o ESO)
    sourceNamespace: acm-credentials
    sourceSecret: ""
    clusterSecretStore: acm-credentials-hub
    refreshInterval: "1h"
    copySshKey: true
    existingCredentialsSecret: ""
    existingPullSecret: ""
    existingSshPrivateKeySecret: ""
  publish: Internal
  fips: false
  azure:
    region: ""
    cloudName: AzurePublicCloud
    baseDomainResourceGroupName: ""
    networkResourceGroupName: ""
    virtualNetwork: ""
    controlPlaneSubnet: ""
    computeSubnet: ""
    outboundType: UserDefinedRouting
  networking:
    networkType: OVNKubernetes
    clusterNetwork: "10.128.0.0/14"
    hostPrefix: 23
    serviceNetwork: "172.30.0.0/16"
    machineNetwork: ""
  controlPlane:
    replicas: 3
    type: Standard_D8s_v3
    osDisk:
      diskSizeGB: 1024
      diskType: Premium_LRS
    zones: ["1", "2", "3"]
  compute:
    replicas: 3
    type: Standard_D4s_v3
    osDisk:
      diskSizeGB: 512
      diskType: Premium_LRS
    zones: ["1", "2", "3"]

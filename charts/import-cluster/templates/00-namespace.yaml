apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.clusterName }}
  labels:
    # Boas práticas: marcar o namespace como parte do cluster
    cluster.open-cluster-management.io/managedCluster: {{ .Values.clusterName }}

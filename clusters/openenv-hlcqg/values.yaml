clusterName: "openenv-hlcqg"
cloudType: "Azure"
baseDomain: "westus.aroapp.io"
labels:
  env: "prod"
  region: "eastus"
  vendor: "OpenShift"
  cloud: "Azure"
  criticality: "high"
  apps.open-cluster-management.io/replicate-to-argocd: "true"
  compliance: "pci-dss"
addons:
  policyController: true
  searchCollector: true
  applicationManager: true
#cloudCredentials:
#  subscriptionId: "REPLACE_IN_CLUSTER"
#  tenantId: "REPLACE_IN_CLUSTER"
#  clientId: "REPLACE_IN_CLUSTER"
#  clientSecret: "REPLACE_IN_CLUSTER"

externalSecrets:
  vaultKeys:
    clientId: "CLIENTID"     # Nome da chave dentro da Azure Vault
    clientSecret: "CLIENTOPASSWD"   # Nome da chave dentro da Azure Vault
    tenantId: "TENANT"
    subscriptionId: "SUBSCRIPTION"
